services:
  db:
    image: postgres
    environment: 
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: db
    ports: 
      - "5432:5432"
    volumes:
      - "postgres_data:/var/lib/postgresql"
volumes:
  postgres_data: